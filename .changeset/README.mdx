# Changesets

This folder is managed by `@changesets/cli`. Run `bun run changeset` after you make changes so we can version updates automatically. Learn more at [https://github.com/changesets/changesets](https://github.com/changesets/changesets).
